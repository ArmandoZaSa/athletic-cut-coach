// Athletic Cut Coach v4.6 — provenance-aware streaming Apple Health importer
(function(){
 const CHUNK=4*1024*1024,attrRe=/([A-Za-z0-9_:.-]+)="([^"]*)"/g;
 const attrs=t=>{const o={};let m;attrRe.lastIndex=0;while((m=attrRe.exec(t)))o[m[1]]=m[2].replace(/&quot;/g,'"').replace(/&amp;/g,'&');return o};
 const dkey=s=>{const d=new Date(s);return Number.isNaN(d.getTime())?String(s||'').slice(0,10):`${d.getFullYear()}-${String(d.getMonth()+1).padStart(2,'0')}-${String(d.getDate()).padStart(2,'0')}`};
 const sleepKey=(start,end)=>dkey(end||start);
 const emptyDay=()=>({weight:null,bodyfat:null,bmi:null,waist:null,steps:0,distance:0,flights:0,activeEnergy:0,restingEnergy:0,exerciseMinutes:0,standMinutes:0,heartRate:null,restingHR:null,walkingHR:null,hrv:null,oxygen:null,respiratory:null,sleep:0});
 const statShape=()=>({records:0,days:new Set(),weight:0,bodyfat:0,bmi:0,waist:0,steps:0,distance:0,flights:0,activeEnergy:0,restingEnergy:0,exerciseMinutes:0,standMinutes:0,heartRate:0,restingHR:0,walkingHR:0,hrv:0,oxygen:0,respiratory:0,sleep:0,workouts:0});
 const sourceShape=()=>({steps:0,distance:0,flights:0,activeEnergy:0,restingEnergy:0,exerciseMinutes:0,standMinutes:0,sleep:0,hrvSum:0,hrvN:0});
 const accNew=()=>({daily:{},provenance:{},sources:new Set(),sourceDaily:{},sourceStats:{},workouts:[],records:0});
 const stats=(a,s)=>(a.sourceStats[s]??=statShape());const sb=(a,s,d)=>(a.sourceDaily[s]??={},a.sourceDaily[s][d]??=sourceShape(),a.sourceDaily[s][d]);
 function latest(a,day,key,val,t,src){const d=a.daily[day]??=emptyDay();if(!d[key]||t>d[key].t){d[key]={v:val,t,src};a.provenance[day]??={};a.provenance[day][key]={source:src,time:t}}}
 function rec(tag,a){const x=attrs(tag),type=x.type||'',src=x.sourceName||'Desconocido',start=x.startDate||x.creationDate||'',end=x.endDate||start,v=parseFloat(x.value),unit=(x.unit||'').toLowerCase();if(!start)return;a.records++;a.sources.add(src);const day=dkey(start),b=sb(a,src,day),s=stats(a,src);s.records++;s.days.add(day);
   if(type.includes('BodyMass')&&!type.includes('Index')&&isFinite(v)){latest(a,day,'weight',unit==='lb'?v*.45359237:v,start,src);s.weight++}
   else if(type.includes('BodyFatPercentage')&&isFinite(v)){latest(a,day,'bodyfat',v<=1?v*100:v,start,src);s.bodyfat++}
   else if(type.includes('BodyMassIndex')&&isFinite(v)){latest(a,day,'bmi',v,start,src);s.bmi++}
   else if(type.includes('WaistCircumference')&&isFinite(v)){latest(a,day,'waist',unit==='m'?v*100:unit==='in'?v*2.54:v,start,src);s.waist++}
   else if(type.includes('StepCount')&&isFinite(v)){b.steps+=v;s.steps++}
   else if(type.includes('DistanceWalkingRunning')&&isFinite(v)){b.distance+=unit==='m'?v/1000:unit==='mi'?v*1.609344:v;s.distance++}
   else if(type.includes('FlightsClimbed')&&isFinite(v)){b.flights+=v;s.flights++}
   else if(type.includes('ActiveEnergyBurned')&&isFinite(v)){b.activeEnergy+=unit.includes('kj')?v/4.184:v;s.activeEnergy++}
   else if(type.includes('BasalEnergyBurned')&&isFinite(v)){b.restingEnergy+=unit.includes('kj')?v/4.184:v;s.restingEnergy++}
   else if(type.includes('AppleExerciseTime')&&isFinite(v)){b.exerciseMinutes+=unit.includes('hr')?v*60:unit.includes('s')?v/60:v;s.exerciseMinutes++}
   else if(type.includes('AppleStandTime')&&isFinite(v)){b.standMinutes+=unit.includes('hr')?v*60:unit.includes('s')?v/60:v;s.standMinutes++}
   else if(type.includes('RestingHeartRate')&&isFinite(v)){latest(a,day,'restingHR',v,start,src);s.restingHR++}
   else if(type.includes('WalkingHeartRateAverage')&&isFinite(v)){latest(a,day,'walkingHR',v,start,src);s.walkingHR++}
   else if(type.includes('HeartRateVariabilitySDNN')&&isFinite(v)){const ms=unit==='s'?v*1000:v;b.hrvSum+=ms;b.hrvN++;s.hrv++}
   else if(type.includes('OxygenSaturation')&&isFinite(v)){latest(a,day,'oxygen',v<=1?v*100:v,start,src);s.oxygen++}
   else if(type.includes('RespiratoryRate')&&isFinite(v)){latest(a,day,'respiratory',v,start,src);s.respiratory++}
   else if(type.endsWith('HeartRate')&&isFinite(v)){latest(a,day,'heartRate',v,start,src);s.heartRate++}
   if(type.includes('SleepAnalysis')&&end){const val=String(x.value||'').toLowerCase(),asleep=val.includes('asleep')||val.includes('core')||val.includes('deep')||val.includes('rem');if(asleep){const h=(new Date(end)-new Date(start))/36e5;if(isFinite(h)&&h>0&&h<=24){const sd=sleepKey(start,end),bs=sb(a,src,sd);bs.sleep+=h;s.sleep++;s.days.add(sd)}}}
 }
 function work(tag,a){const x=attrs(tag),src=x.sourceName||'Desconocido',start=x.startDate||'',raw=x.workoutActivityType||'Workout';if(!start)return;a.sources.add(src);const s=stats(a,src);s.workouts++;s.days.add(dkey(start));let dur=parseFloat(x.duration)||0,du=(x.durationUnit||'min').toLowerCase();dur=du.includes('hr')?dur*60:du.includes('s')?dur/60:dur;let en=parseFloat(x.totalEnergyBurned)||0;if((x.totalEnergyBurnedUnit||'').toLowerCase().includes('kj'))en/=4.184;a.workouts.push({date:dkey(start),start,type:raw.replace(/^HKWorkoutActivityType/,'').replace(/([a-z])([A-Z])/g,'$1 $2'),source:src,duration:Math.round(dur),energy:Math.round(en)})}
 function line(t,a){let m;const rr=/<Record\b[^>]*\/?>/g;while((m=rr.exec(t)))rec(m[0],a);const wr=/<Workout\b[^>]*>/g;while((m=wr.exec(t)))work(m[0],a)}
 const pref=(metric,src)=>{src=src.toLowerCase();if(metric==='sleep')return src.includes('huawei')?0:src.includes('health')?1:2;if(metric==='hrv')return(src.includes('watch')||src.includes('health')||src.includes('iphone'))?0:src.includes('huawei')?1:2;if(['steps','distance','flights','exerciseMinutes','standMinutes'].includes(metric))return(src.includes('watch')||src.includes('iphone')||src.includes('health'))?0:src.includes('huawei')?1:2;return 1};
 function chooseAgg(a,day,metric){const candidates=[];for(const[src,days]of Object.entries(a.sourceDaily)){const b=days[day];if(!b)continue;let v=metric==='hrv'?(b.hrvN?b.hrvSum/b.hrvN:0):b[metric];if(v>0)candidates.push({src,v,p:pref(metric,src)})}if(!candidates.length)return{v:0,src:null};candidates.sort((x,y)=>x.p-y.p||y.v-x.v);return{v:candidates[0].v,src:candidates[0].src}}
 function finish(a){const allDays=new Set(Object.keys(a.daily));for(const days of Object.values(a.sourceDaily))for(const day of Object.keys(days))allDays.add(day);for(const day of allDays){const d=a.daily[day]??=emptyDay();a.provenance[day]??={};for(const k of['steps','distance','flights','activeEnergy','restingEnergy','exerciseMinutes','standMinutes','sleep','hrv']){const c=chooseAgg(a,day,k);d[k]=c.v;if(c.src)a.provenance[day][k]={source:c.src,policy:k==='sleep'?'Huawei preferente':k==='hrv'?'Apple/Health preferente':'anti-duplicado por fuente'}}for(const k of['weight','bodyfat','bmi','waist','heartRate','restingHR','walkingHR','oxygen','respiratory'])if(d[k]){const obj=d[k];d[k]=+obj.v.toFixed(k==='weight'?2:1)}d.steps=Math.round(d.steps);d.distance=+d.distance.toFixed(2);d.flights=Math.round(d.flights);d.activeEnergy=Math.round(d.activeEnergy);d.restingEnergy=Math.round(d.restingEnergy);d.exerciseMinutes=Math.round(d.exerciseMinutes);d.standMinutes=Math.round(d.standMinutes);d.sleep=+d.sleep.toFixed(2);d.hrv=d.hrv?+d.hrv.toFixed(1):0}
   a.workouts.sort((x,y)=>y.start.localeCompare(x.start));const ss={};for(const[src,s]of Object.entries(a.sourceStats)){ss[src]={};for(const[k,v]of Object.entries(s))ss[src][k]=v instanceof Set?v.size:v}return{daily:a.daily,provenance:a.provenance,workouts:a.workouts.slice(0,1500),sources:[...a.sources].sort(),sourceStats:ss,lastImport:new Date().toISOString(),stats:{records:a.records,days:Object.keys(a.daily).length},schema:46}}
 async function parse(file){const a=accNew(),dec=new TextDecoder('utf-8');let off=0,carry='';while(off<file.size){const end=Math.min(file.size,off+CHUNK),buf=await file.slice(off,end).arrayBuffer();carry+=dec.decode(buf,{stream:end<file.size});off=end;const ls=carry.split(/\r?\n/);carry=ls.pop()||'';for(const l of ls)line(l,a);window.setImp?.(8+Math.floor(off/file.size*88),`Procesando Salud… ${Math.round(off/file.size*100)}% · ${a.records.toLocaleString()} registros`);await new Promise(r=>setTimeout(r,0))}if(carry)line(carry,a);return finish(a)}
 window.handleHealthFile=async e=>{const f=e.target.files?.[0];if(!f)return;try{if(!f.name.toLowerCase().endsWith('.xml'))throw Error('Selecciona export.xml de Apple Salud.');window.setImp?.(5,`Leyendo ${(f.size/1024/1024/1024).toFixed(2)} GB…`);const p=await parse(f);db.health=p;window.mergeHealth?.();window.persist?.();window.setImp?.(100,`Listo: ${p.stats.records.toLocaleString()} registros · ${p.stats.days} días · ${p.sources.length} fuentes`);window.renderSourceInsights?.();window.renderHealthV45?.();window.toast?.('Apple Salud importado con procedencia');e.target.value=''}catch(err){console.error(err);window.setImp?.(0,err.message||'No se pudo importar');window.toast?.('No se pudo importar Salud')}};
})();