// Athletic Cut Coach v4.8.2 — single Health bootstrap + diagnostics UI
(function(){
 const VERSION='v4.8.2';
 const labels={weight:'Peso',bodyfat:'Grasa',bmi:'IMC',waist:'Cintura',steps:'Pasos',distance:'Distancia',flights:'Pisos',activeEnergy:'Energía activa',restingEnergy:'Energía reposo',exerciseMinutes:'Ejercicio',standMinutes:'De pie',heartRate:'FC',restingHR:'FC reposo',walkingHR:'FC caminando',hrv:'HRV',oxygen:'SpO₂',respiratory:'Respiración',sleep:'Sueño',workouts:'Entrenamientos'};
 const days=()=>Object.keys(db?.health?.daily||{}).sort().reverse();
 function latest(k){for(const d of days()){const v=db.health.daily[d]?.[k];if(v!==null&&v!==undefined&&Number(v)!==0)return{v,date:d,source:db.health?.provenance?.[d]?.[k]?.source||null}}return null}
 function fmt(k,v){if(k==='sleep'){const h=Math.floor(v),m=Math.round((v-h)*60);return`${h} h ${m} min`}if(k==='weight')return`${Number(v).toFixed(1)} kg`;if(k==='bodyfat')return`${Number(v).toFixed(1)} %`;if(k==='distance')return`${Number(v).toFixed(1)} km`;if(k==='activeEnergy'||k==='restingEnergy')return`${Math.round(v)} kcal`;if(k==='exerciseMinutes'||k==='standMinutes')return`${Math.round(v)} min`;if(k==='heartRate'||k==='restingHR'||k==='walkingHR')return`${Math.round(v)} lpm`;if(k==='hrv')return`${Math.round(v)} ms`;if(k==='oxygen')return`${Number(v).toFixed(0)} %`;if(k==='respiratory')return`${Number(v).toFixed(1)}/min`;return String(v)}
 function shortType(t){return String(t||'').replace(/^HK(?:Quantity|Category|Characteristic|Correlation)TypeIdentifier/,'').replace(/^HKDataTypeIdentifier/,'')}
 window.renderHealthKitDiagnostics=function(){const salud=document.getElementById('salud');if(!salud)return;let box=document.getElementById('healthkitDiagnostics');if(!box){box=document.createElement('div');box.id='healthkitDiagnostics';box.className='card';salud.querySelector('.card')?.after(box)}const dg=db?.health?.diagnostics||{},types=dg.relevantTypes||[];if(!types.length){box.innerHTML='<h2>Diagnóstico HealthKit</h2><div class="small">Sin censo persistente. Importa export.xml con v4.8.2.</div>';return}const wanted=[/sleep/i,/variability|hrv/i,/restingheart/i,/activeenergy/i,/distance/i,/exercisetime/i,/standtime/i],found=types.filter(x=>wanted.some(r=>r.test(x.type)));box.innerHTML=`<div style="display:flex;justify-content:space-between;gap:10px;align-items:center"><h2 style="margin:0">Diagnóstico HealthKit</h2><span class="badge ${found.length?'ok':''}">${found.length?'Tipos objetivo encontrados':'Tipos objetivo ausentes del XML'}</span></div><div class="small" style="margin:6px 0 12px">${dg.distinctTypes||types.length} tipos · IndexedDB · schema ${db.health?.schema||'—'}</div>${types.slice(0,50).map(x=>`<div class="hist"><b>${shortType(x.type)}</b><br><span class="small">${Number(x.count||0).toLocaleString('es-MX')} registros · <code style="font-size:10px;word-break:break-all">${x.type}</code></span></div>`).join('')}`}
 window.renderSourceInsights=function(){const list=document.querySelector('.sourceList');if(!list)return;let box=document.getElementById('sourceInsights');if(!box){box=document.createElement('div');box.id='sourceInsights';box.style.marginTop='14px';list.parentElement.appendChild(box)}const stats=db?.health?.sourceStats||{},names=Object.keys(stats);if(!names.length){box.innerHTML='<div class="small">Importa export.xml para ver procedencia y conteos por fuente.</div>';return}box.innerHTML='<div class="eyebrow" style="margin:4px 0 10px">Qué aporta cada fuente</div>'+names.filter(n=>/huawei|gravl|health|iphone|watch|activity/i.test(n)).slice(0,12).map(name=>{const s=stats[name],items=Object.entries(labels).filter(([k])=>Number(s[k])>0).map(([k,l])=>`${l}: ${Number(s[k]).toLocaleString('es-MX')}`).join(' · ');return`<div class="hist"><b>${name}</b><br><span class="small">${items||'Sin métricas mapeadas'} · ${s.days||0} días</span></div>`}).join('')}
 window.renderHealthV45=function(){const inicio=document.getElementById('inicio');if(!inicio)return;let card=document.getElementById('healthExpanded');if(!card){card=document.createElement('div');card.id='healthExpanded';card.className='card';inicio.insertBefore(card,document.querySelector('#inicio .card'))}const keys=[['sleep','Sueño'],['hrv','HRV'],['restingHR','FC reposo'],['activeEnergy','Energía activa'],['distance','Distancia'],['exerciseMinutes','Ejercicio'],['oxygen','SpO₂'],['respiratory','Respiración'],['weight','Peso'],['bodyfat','Grasa corporal']];card.innerHTML=`<div style="display:flex;justify-content:space-between;align-items:center"><h2 style="margin:0">Salud ampliada</h2><span class="badge ok">${VERSION}</span></div><div class="small" style="margin:6px 0 12px">Datos persistentes en IndexedDB con fecha y procedencia.</div><div class="grid">${keys.map(([k,l])=>{const x=latest(k);return`<div class="metric"><b style="font-size:18px">${x?fmt(k,x.v):'No disponible'}</b><span>${l}${x?` · ${x.date}${x.source?' · '+x.source:''}`:''}</span></div>`}).join('')}</div>`}
 function rerender(){window.renderSourceInsights?.();window.renderHealthV45?.();window.renderHealthKitDiagnostics?.()}
 const old=window.render;if(typeof old==='function'&&!old.__v482){const w=function(){const r=old.apply(this,arguments);queueMicrotask(rerender);return r};w.__v482=true;window.render=w}
 function loadCore(){const s=document.createElement('script');s.src='./health-core-v482.js?v=482';s.dataset.healthCore482='1';s.onload=async()=>{await window.CutCoachHealthCore.installBrowser();rerender();const p=document.querySelector('#importModal .small');if(p)p.innerHTML='Selecciona directamente <b>export.xml</b>. v4.8.2 procesa Record, Workout y ActivitySummary, y guarda Salud en IndexedDB.'};document.body.appendChild(s)}
 if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',()=>{loadCore();rerender()});else{loadCore();rerender()}
})();